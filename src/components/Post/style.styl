.Post {
  & {
    display: flex
    flex-direction: column
    justify-content: space-between
    height: 100%
  }

  & &__media {
    height: 300px
    bg()
  }
  
  &__title {
    text-transform: capitalize
  }
}
